# 工具需求草案

## 1. 产品定位

一个完全本地运行的网页归档管理工具，解决“已经保存，但很难再次找到和阅读”的问题。

它不是新的浏览器收藏夹，也不要求把原件转换成另一种格式。原始 HTML/MHTML 是事实源，数据库负责检索、分类和状态管理。

## 2. 第一版目标

第一版应优先实现：

1. 扫描指定目录，增量发现新增、修改、移动和删除的文件。
2. 解析 HTML、MHTML/MHT 的标题、原 URL、正文和保存时间。
3. 本地全文检索，支持中文和技术关键词。
4. 按分类、标签、来源、保存时间、文件格式和阅读状态筛选。
5. 查看网页摘要、元数据和原始文件路径。
6. 一键用安全方式打开本地原件。
7. 编辑分类和标签，并支持批量确认分类建议。
8. 展示重复文件、同源多版本和解析失败项。

## 3. 暂不进入第一版

- 自动删除重复文件。
- 自动大规模移动或重命名原件。
- 云同步和多人协作。
- 向第三方服务上传正文进行分类。
- 自动抓取网页最新版本。
- 对所有 PDF、Office、XMind 做全文解析。
- 复杂知识图谱和聊天问答。

## 4. 关键使用场景

### 找回技术资料

输入 `Nacos 离线安装 CentOS`，即使文件名不完全包含这些词，也能通过正文命中，并按保存时间和来源筛选。

### 查看失效网页

搜索结果展示原始 URL、保存时间和本地文件。原网站失效时仍可打开本地归档。

### 清理暂未分类

筛选 `primary_category = uncategorized`，查看工具建议，批量确认主分类和标签，但不移动原件。

### 处理多次保存

同一个 URL 在不同日期保存多次时，默认展示最新版本，并能进入历史版本列表。

## 5. 推荐架构

结合使用者是后端开发工程师，建议第一版采用简单、可维护的本地架构：

```text
文件扫描器
   ↓
格式解析器（HTML / MHTML）
   ↓
元数据与正文标准化
   ↓
SQLite + FTS5 全文索引
   ↓
本地 HTTP API
   ↓
浏览器管理界面
```

技术选择建议：

- 后端：Java/Spring Boot 或 Python/FastAPI 均可；如果强调长期维护和个人熟悉度，优先 Java。
- 数据库：SQLite，减少部署成本；全文检索先使用 FTS5。
- 前端：轻量 Vue/React，或第一版使用服务端模板快速验证。
- 文件监听：首次全量扫描 + 定时增量扫描，文件系统监听作为后续增强。
- 部署：单机进程，仅监听 `127.0.0.1`。

## 6. 非功能要求

### 数据安全

- 默认离线运行。
- 不修改原件。
- 所有写操作有审计日志。
- 数据库可重建，原始归档不可被索引工具删除。

### 性能

- 以当前约 783 个文件、3.4GB 为基线，并预留到 10 万条记录。
- 大文件流式读取，不一次性加载全部内容。
- 正文抽取和指纹计算可断点续跑。
- 增量扫描只处理发生变化的文件。

### 可维护性

- 解析器按格式和站点解耦。
- 分类规则带版本号。
- 失败任务可重试。
- 数据库提供导出 JSONL/CSV 的能力，避免工具锁定。

## 7. 后续增强方向

- 浏览器扩展：保存网页时直接写入标签和备注。
- 自动摘要：优先使用本地模型，或在明确授权后调用外部模型。
- NoteHub 联动：从网页生成知识笔记，并保留资产反向链接。
- OCR：处理截图和扫描 PDF。
- 相似内容聚类：按主题形成专题阅读列表。
- 定期健康检查：发现损坏文件、丢失路径和索引漂移。

## 8. 第一版完成标准

第一版不以“所有文件都分好类”为完成标准，而以以下结果为准：

- 能完整扫描当前目录且不修改原件。
- HTML/MHTML 的主要内容可被检索。
- 大多数网页能正确显示标题、来源和保存时间。
- 用户能在数十秒内找到过去保存的目标网页。
- 新增网页可以自动进入待整理队列。
- 所有分类结果可人工修正，且不会因目录调整而失效。

