# 现状盘点

> 盘点日期：2026-08-15  
> 盘点方式：只读扫描，未修改源目录

## 1. 总体规模

| 指标 | 当前值 |
| --- | ---: |
| 总体积 | 约 3.4GB |
| 文件数 | 783 |
| 目录数 | 212 |
| HTML | 300 |
| MHTML / MHT | 233（225 + 8） |
| Markdown | 66 |
| `.DS_Store` | 126 |
| 图片 | 37（JPG 19、PNG 16、GIF 2） |
| 其他 | XMind、PDF、Excel、PPTX、DOC、RAR、日志等 |

说明：扩展名只能说明文件表面格式。实施前仍需通过 MIME/文件头再次识别，防止扩展名不准确。

## 2. 顶层目录概况

当前顶层存在 21 个业务目录，并有一个 `.DS_Store` 文件。主要目录如下：

| 目录 | 文件数 | 体积 | 初步判断 |
| --- | ---: | ---: | --- |
| `暂未分类` | 157 | 约 1.2GB | 新近内容集中区，主题高度混杂 |
| `video` | 67 | 约 1.0GB | 以视频页面为主，已有二级主题分类 |
| `article-viewed` | 139 | 约 359MB | “已读状态”与主题分类混用 |
| `KunNoteData` | 69 | 约 243MB | 笔记、文档和旧归档混合 |
| `other` | 27 | 约 88MB | 兜底目录，语义弱 |
| `topic` | 20 | 约 85MB | 专题内容，但边界不清楚 |
| `LifeNoteData` | 46 | 约 64MB | 生活资料，包含私密、健康、日志等 |
| `it-improve` | 30 | 约 63MB | 后端技能提升类内容 |
| `it` | 9 | 约 58MB | IT 常识、设备、系统、网络等 |
| `GoodBlog` | 32 | 约 51MB | 以内容质量为依据的分类 |
| `ai-chat` | 65 | 约 50MB | AI 对话记录，内部仍按主题分类 |
| 其他目录 | 122 | 约 200MB+ | `tech`、`job`、`finance`、`tool`、`ai` 等 |

文件数是递归统计，因此包含目录内的非网页附件。

## 3. 现有分类的主要问题

### 3.1 分类维度混用

当前目录同时表达了不同维度：

- 内容主题：`tech`、`finance`、`life`、`ai`。
- 内容形式：`video`、`ai-chat`。
- 阅读状态：`article-viewed`。
- 内容质量：`GoodBlog`。
- 使用目的：`it-improve`、`Knowledge`。
- 来源或历史系统：`KunNoteData`、`LifeNoteData`、`FinanceData`。
- 无法判断：`other`、`topic`、`暂未分类`。

一个网页往往同时属于多个维度。例如一篇“B 站上的 Java 面试视频”既是 `video`，又是 `tech`、`job`，还可能属于 `article-viewed`。仅靠目录树无法稳定表达这种关系。

### 3.2 同义或重叠目录较多

明显存在以下重叠：

- `it`、`tech`、`it-improve`。
- `life`、`LifeNoteData`。
- `finance`、`FinanceData`。
- `ai`、`ai-chat`、各目录下的 `AI` 子目录。
- `Knowledge`、`GoodBlog`、`article-viewed`。

继续增加目录会让“这个文件到底放哪里”成为主要成本。

### 3.3 文件名承担了过多元数据

常见文件名包含：网页标题、站点名、搜索结果标记、消息数量、抓取日期和时间。问题包括：

- 文件名过长，跨平台迁移风险高。
- 中文全角冒号、竖线、特殊符号较多。
- 同一抓取时间存在多种格式。
- 知乎消息数等临时 UI 文本进入文件名。
- 标题变化后难以识别同一 URL 的多个版本。

### 3.4 网页与个人资料混存

目录内不只有网页，还存在 Markdown、XMind、PDF、Excel、PPTX、DOC、RAR、图片和日志。尤其 `private`、工作资料、健康资料等内容不应由普通网页整理规则自动搬运。

### 3.5 体积不能只按文件数判断

少数 HTML 单文件达到数十 MB，说明部分网页可能以内嵌资源方式保存。最大的普通归档文件包括约 96MB 的 RAR，以及约 20MB～86MB 的多个 HTML 页面。后续计算指纹、抽取正文和生成预览时必须采用流式处理和大小限制。

## 4. 格式特征与风险

### HTML

抽样文件为 UTF-8 HTML，存在超长行，常见 `<title>` 可正常提取。部分文件可能由 SingleFile 一类工具保存，图片、样式和脚本直接内嵌，因此单文件很大。

风险：

- 不能通过“格式化 HTML”来整理，否则会改变原件。
- 正文抽取需限制脚本、样式和 Base64 数据参与索引。
- 直接在浏览器打开时，应防止归档脚本访问外网或执行危险操作。

### MHTML / MHT

数量接近 HTML，是不可忽略的主格式。它本质上是 MIME 多段归档，正文、图片、样式可能封装在同一个文件中。

风险：

- 普通文本工具不一定能正确解析。
- 不能只改扩展名当作 HTML 使用。
- 工具需单独实现或引入成熟的 MHTML 解析路径。

### Markdown 与其他附件

这部分更像个人知识资产，不应与网页执行相同的标题清洗、正文抽取和去重策略。第一阶段只登记，不改名、不移动。

## 5. 需要补充的完整清单

正式实施前，应生成机器可读的 `inventory.jsonl` 或 SQLite 清单，至少包含：

- 原始绝对路径和相对路径。
- 文件名、扩展名、实际 MIME 类型、大小。
- 文件修改时间、可推断的保存时间。
- SHA-256 内容指纹。
- HTML/MHTML 标题、原始 URL、站点域名。
- 是否能成功解析、是否疑似损坏。
- 是否属于敏感目录或非网页资产。

当前文档是方案级盘点，不替代实施时生成的逐文件审计清单。
