# 实施计划与验收方案

## 1. 实施边界

> 2026-08-15 已确认：采用 8 个主分类；当前网页文件不设置私密内容扫描限制；先建立索引，暂不物理搬迁。

在用户审阅本方案之前：

- 不移动原文件。
- 不重命名原文件。
- 不删除 `.DS_Store` 或重复文件。
- 不改写 HTML/MHTML 内容。
- 不上传任何文件到云端或第三方 AI 服务。

## 2. 分阶段计划

### 阶段 0：冻结规则并备份

目标：确认范围和安全边界。

任务：

1. 确认源目录与备份状态。
2. 确认是否允许扫描私密目录正文；默认只记录基础文件信息。
3. 确认第一批试点范围，建议只选 `暂未分类` 中 20～30 个普通 HTML。
4. 确认分类表、标签规范和敏感目录规则。

产出：已确认的规则版本和试点清单。

### 阶段 1：生成只读资产清单

目标：建立完整、可复核的事实基线。

任务：

1. 遍历文件并记录路径、大小、时间、扩展名和 MIME。
2. 流式计算 SHA-256。
3. 标记完全重复文件。
4. 输出 SQLite 数据库和 JSONL/CSV 审计文件。
5. 记录扫描失败项，不中断全量任务。

产出：

```text
data/catalog.sqlite
reports/inventory.jsonl
reports/scan-summary.md
reports/scan-errors.csv
```

### 阶段 2：元数据与正文抽取

目标：让网页可以搜索，而不是只依赖文件名。

任务：

1. 分别解析 HTML 与 MHTML/MHT。
2. 抽取标题、原 URL、域名、正文、作者和页面时间。
3. 清除脚本、样式、Base64 资源对全文索引的干扰。
4. 对超大文件设置流式处理、超时和内存上限。
5. 对 Markdown 和其他附件先只登记元数据。

产出：可全文搜索的本地索引，以及解析成功率报告。

### 阶段 3：自动分类建议

目标：生成建议，不直接改文件。

任务：

1. 按规则识别来源和资产类型。
2. 使用标题、正文关键词和路径生成主分类与标签。
3. 每个建议记录依据、置信度和规则版本。
4. 生成待人工确认列表。

产出：分类建议报告和审阅界面/可编辑表格。

### 阶段 4：小批量试整理

目标：验证规则可用性和回滚能力。

建议只处理 20～30 个普通 HTML，流程如下：

1. 生成 dry-run 操作清单。
2. 人工逐条确认目标分类和目标文件名。
3. 复制到独立的试验目录，不删除原件。
4. 校验源文件与副本 SHA-256 一致。
5. 随机打开至少 10 个页面验证可读性。
6. 执行一次完整回滚演练。

### 阶段 5：全量逻辑整理

目标：在不移动原件的情况下完成可用的管理体验。

任务：

1. 建立全文检索。
2. 提供按主分类、标签、来源、时间、格式、状态筛选。
3. 提供原文件打开、路径定位和元数据修订。
4. 提供重复组、解析失败和待确认队列。

到这里，通常已经不再需要大规模物理迁移。

### 阶段 6：可选的物理重组

只有在逻辑整理使用一段时间后仍有明确需求，才执行物理重组：

1. 根据已确认的元数据生成目标路径。
2. 先复制后校验。
3. 输出旧路径到新路径映射。
4. 保留一段观察期。
5. 旧目录的删除必须再次单独确认。

## 3. 每批操作清单

任何批量变化都必须先产生清单：

| 字段 | 说明 |
| --- | --- |
| `operation_id` | 本次操作唯一编号 |
| `asset_id` | 资产唯一编号 |
| `source_path` | 原始路径 |
| `target_path` | 目标路径 |
| `action` | copy / move / rename / skip |
| `reason` | 分类或重命名依据 |
| `sha256_before` | 操作前指纹 |
| `sha256_after` | 操作后指纹 |
| `status` | planned / success / failed / rolled_back |

默认先执行 `dry-run`，只有人工确认后的清单才能实际执行。

## 4. 验收标准

### 清单阶段

- 文件数、总字节数与独立只读统计一致。
- 100% 文件有原始路径、大小和扫描状态。
- 指纹失败、权限失败等异常都有单独报告。
- 源目录在扫描前后内容指纹和文件数不因工具发生变化。

### 解析阶段

- HTML 和 MHTML 分别统计成功率，不能合并掩盖问题。
- 抽样页面的标题、原 URL 和正文可人工核对。
- 超大文件不会导致任务整体崩溃。
- 解析失败不改动原件，并可重复执行。

### 分类阶段

- 每个自动分类都可解释、可修改。
- 低置信度内容进入 `uncategorized` 或待确认队列。
- 敏感目录不参与未经授权的正文分类。
- 状态、来源、形式不再误作唯一主题分类。

### 试整理阶段

- 复制后的文件指纹与原件一致。
- 抽样页面能离线打开。
- 回滚后目录状态与实施前一致。
- 无任何“自动删除重复文件”行为。

## 5. 回滚策略

第一阶段只有索引文件和报告，可以直接停止使用，不影响原目录。

如果进入复制/迁移阶段：

- 每批使用独立 `operation_id`。
- 操作日志必须在文件变化之前落盘。
- 回滚按映射表逆序执行。
- 目标已存在或指纹不一致时停止，不覆盖。
- 原目录删除不包含在普通回滚脚本中，必须单独授权。

## 6. 建议的首次实施范围

首批建议从 `暂未分类` 选取：

- 10 个普通技术 HTML。
- 5 个知乎/CSDN 等大体积单文件 HTML。
- 5 个视频详情页。
- 5 个 MHTML/MHT。
- 2～5 个搜索结果页或 AI 对话页。

首批排除：私密文件、工作合同、健康资料、Office 文件、压缩包以及超大异常文件。

## 7. 已确认决策

1. 已接受“先建索引、暂不物理搬家”的总体路线。
2. 已接受本文定义的 8 个主分类。
3. 当前网页文件不设置私密正文扫描限制。
4. HTML 与 MHTML 均纳入索引范围。
5. 先执行全量只读资产清单；正文解析和分类准确率再按抽样结果迭代。
