# 运行与恢复指南

## 1. 环境要求

- macOS，Python 3.9 或更高版本。
- 不依赖第三方 Python 包。
- 网页归档目录和 `data/catalog.sqlite` 已存在。

首次构建索引：

```bash
python3 scripts/build_catalog.py --source '/Users/wankun/allbak/网页' --output .
python3 scripts/build_search_index.py --database data/catalog.sqlite
python3 scripts/classify_catalog.py --apply
```

## 2. 配置与启动

默认可直接启动：

```bash
python3 app.py
```

服务只允许监听 `127.0.0.1`、`localhost` 或 `::1`，不会暴露到局域网。

自定义配置：

```bash
cp config.example.json config.json
```

`config.json` 支持：

| 字段 | 默认值 | 说明 |
| --- | --- | --- |
| `database` | `data/catalog.sqlite` | SQLite 索引路径 |
| `host` | `127.0.0.1` | 本机监听地址 |
| `port` | `8765` | HTTP 端口 |
| `log_file` | `logs/app.log` | 运行日志 |

命令行参数优先于配置文件：

```bash
python3 app.py --port 8877 --log-file logs/dev.log
```

## 3. 启动前检查和日志

只检查、不启动服务：

```bash
python3 app.py --check
```

检查内容包括 SQLite 完整性、外键、孤立正文、全文索引和维护队列。正常时退出码为 0。

运行日志默认写入 `logs/app.log`，单文件达到 2 MiB 后轮转，保留 5 份历史日志。`logs/` 不提交 Git。

## 4. 日常同步

可以在页面点击“同步归档”，也可以运行：

```bash
python3 scripts/incremental_scan.py \
  --source '/Users/wankun/allbak/网页' \
  --database data/catalog.sqlite
```

同步不会移动或修改网页原件。

## 5. 备份与恢复

在页面“数据保全袋”点击“创建本地快照”，产物位于：

```text
data/backups/{backup_id}/catalog.sqlite
data/backups/{backup_id}/overrides-bundle.json
```

先在系统临时目录中演练恢复，不覆盖当前数据库：

```bash
python3 scripts/verify_backup.py \
  --backup data/backups/{backup_id} \
  --current data/catalog.sqlite
```

输出为 `healthy` 且 `comparison.matches` 为 `true`，表示快照完整性和主要记录数量与当前索引一致。

恢复前先停止服务，然后保留当前文件并复制快照：

```bash
cp data/catalog.sqlite data/catalog.before-restore.sqlite
cp data/user-overrides.json data/user-overrides.before-restore.json
cp data/backups/{backup_id}/catalog.sqlite data/catalog.sqlite
```

人工数据可以在页面使用“导入并合并”，不必替换数据库。导入默认保留本地其他记录，未知资产 ID 会跳过。

恢复后检查：

```bash
python3 app.py --check
python3 app.py
```

## 6. 常见问题

- 页面提示服务版本过旧：在终端按 `Ctrl+C` 停止旧进程，再运行 `python3 app.py`。
- 端口被占用：修改 `config.json` 的 `port`，或使用 `python3 app.py --port 8877`。
- 数据库检查失败：停止服务，保留故障文件，从最近快照恢复，不要直接覆盖唯一副本。
- 网页目录迁移：先修改/重建目录元数据并执行增量扫描；不要只在数据库中批量替换路径。
