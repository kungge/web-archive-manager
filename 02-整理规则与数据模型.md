# 整理规则与数据模型

## 1. 总体原则

### 1.1 原件优先

- 原始文件是不可替代的证据副本，不修改文件内容。
- 第一次实施只建立索引，不物理迁移。
- 后续即使迁移，也先复制到新结构并校验，再由人工决定是否清理旧位置。
- 所有自动动作必须可通过清单反向恢复。

### 1.2 逻辑整理优先于物理整理

物理目录只能有一个位置，而内容可以有多个属性。建议采用：

- 一个主分类：表达内容主要主题。
- 多个标签：表达技术栈、来源、形式、状态和用途。
- 一个资产类型：网页、AI 对话、视频页、笔记、附件等。
- 原始路径永久保留在元数据中。

### 1.3 不以“是否已读”决定目录

`已读`、`待读`、`收藏`、`重点`属于状态，应记录在数据库中，不再作为物理目录。

## 2. 推荐主分类

主分类建议控制在 8 类，避免过细：

| 主分类 | 范围示例 |
| --- | --- |
| `technology` | Java、数据库、架构、DevOps、开发工具、操作系统、硬件 |
| `ai` | 大模型、Agent、AI 工具、AI 编程、工作流 |
| `career-work` | 面试、职场、银行项目、工作方法、招聘 |
| `finance-business` | 投资、股票、商业、公司、支付、赚钱项目 |
| `life` | 健康、家庭、城市生活、房产、消费、宠物 |
| `society-culture` | 社会热点、历史、人文、影视、观点文章 |
| `productivity-tools` | 非开发类软件、浏览器、效率工具、RPA |
| `uncategorized` | 暂时无法可靠判断的内容 |

这套分类不要求直接变成中文物理目录。界面可显示中文，内部值保持稳定英文标识。

## 3. 推荐标签维度

标签使用命名空间，避免同名歧义：

```text
topic:java
topic:nacos
topic:ddd
source:zhihu
source:bilibili
format:video-page
format:ai-chat
status:unread
quality:reference
usage:troubleshooting
sensitivity:private
```

自动标签必须带置信度。低于阈值的结果进入人工确认队列，不能直接搬文件。

## 4. 资产类型

| 类型 | 说明 | 第一阶段处理 |
| --- | --- | --- |
| `web-html` | 普通或单文件 HTML | 抽取元数据和正文 |
| `web-mhtml` | MHTML/MHT 网页归档 | MIME 解析后抽取 |
| `video-page` | B 站等视频详情页 | 保存页面，并额外抽取作者、视频标识等 |
| `search-page` | 百度、知乎等搜索结果页 | 可检索，但降低内容优先级 |
| `ai-chat` | AI 对话或生成内容 | 独立类型，注意隐私 |
| `note` | Markdown、日志等个人内容 | 只登记，不自动改动 |
| `attachment` | 图片、PDF、Office、XMind、压缩包 | 只登记，后续按格式扩展 |

## 5. 元数据模型

建议 SQLite 作为本地索引数据库，原始文件仍保留在文件系统。核心记录示例：

```json
{
  "asset_id": "sha256前16位或稳定UUID",
  "original_path": "/Users/wankun/allbak/网页/暂未分类/xxx.html",
  "relative_path": "暂未分类/xxx.html",
  "file_name": "xxx.html",
  "extension": "html",
  "mime_type": "text/html",
  "size_bytes": 123456,
  "sha256": "完整内容指纹",
  "title_raw": "原始标题",
  "title_clean": "清洗后的标题",
  "source_url": "https://example.com/article/1",
  "source_domain": "example.com",
  "saved_at": "2026-08-15T10:30:00+08:00",
  "modified_at": "文件系统时间",
  "asset_type": "web-html",
  "primary_category": "technology",
  "tags": ["topic:java", "source:csdn"],
  "parse_status": "success",
  "duplicate_group": null,
  "sensitivity": "normal"
}
```

数据库至少需要以下逻辑表：

- `assets`：文件和网页核心元数据。
- `tags`、`asset_tags`：多标签。
- `contents`：抽取的纯文本和全文索引。
- `relations`：重复、版本、附件等关系。
- `operations`：每次整理动作及回滚信息。

## 6. 标题和文件名规则

### 标题清洗

清洗只作用于索引字段 `title_clean`，不直接覆盖原文件名：

1. 去掉知乎消息数、通知数等页面状态噪声。
2. 去掉重复站点后缀，但保留 `source_domain`。
3. 统一多余空白和部分全角标点。
4. 保留有语义的版本、年份、产品名和技术名。

### 未来物理文件名

若审阅后确实要重命名，建议：

```text
YYYYMMDD_清洗标题__短ID.html
```

其中短 ID 来源于内容指纹或稳定资产 ID，用于解决同名和多版本问题。文件名应设长度上限，例如 120 个 Unicode 字符；完整标题存数据库。

## 7. 时间识别优先级

保存时间按以下优先级推断，并记录来源：

1. 网页归档内明确的保存时间元数据。
2. 当前文件名末尾的日期时间。
3. 文件系统创建时间。
4. 文件系统修改时间。

不要把文章发布时间误当成保存时间。两者应分别记录。

## 8. 重复与版本规则

### 完全重复

SHA-256 相同的文件标记为完全重复。第一轮仅聚类，不删除。

### 同源多版本

原始 URL 规范化后一致，但指纹不同，标记为同源版本：

- 保留每个版本。
- 记录保存时间。
- 默认展示最新版本，可查看历史版本。

### 近似重复

正文归一化后计算 SimHash/MinHash 或文本相似度。近似重复只提供建议，不能自动删除。

### 搜索页和低价值页面

搜索结果页可以降低搜索权重或标记 `quality:low-signal`，但仍保留，因为其中可能包含已失效结果的摘要。

## 9. 安全分区

以下内容第一阶段禁止自动移动或重命名：

- 路径中含 `private`、`important`、`工作`、合同、健康、账号等敏感含义的文件。
- 非 HTML/MHTML 的个人笔记和附件。
- 解析失败、编码未知或内容指纹计算失败的文件。
- 压缩包以及可能存在内部路径依赖的复合文件。

HTML 预览应默认禁止脚本执行、阻止自动外连，并在隔离上下文中打开。

## 10. 与 NoteHub 的关系

网页原件不建议整体迁入 Git 管理的 NoteHub：体积大、二进制多、更新频繁。推荐关系是：

- `/Users/wankun/allbak/网页`：原始归档库。
- NoteHub：整理规则、项目代码、精选摘要和长期知识笔记。
- 本地索引数据库：网页原件与 NoteHub 笔记之间的桥梁。

对于高价值网页，可以在 NoteHub 中生成一篇摘要或知识笔记，并通过 `asset_id` 或原始路径反向链接原件。

